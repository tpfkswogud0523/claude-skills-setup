# Sentry Project

This repository is mapped to Sentry project `claude-skills-setup` in organization `park-company-dy`.

This repository mainly stores setup scripts and documentation. If executable automation is added later, wire runtime errors to Sentry with `SENTRY_DSN` and keep the DSN out of commits.
